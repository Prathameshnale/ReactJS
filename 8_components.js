//components - components are the repitedly used building blocks used to make UI.

//when compenents needs to make -> when any functionality is need to used more than one time then compenents
// should make.
//e.g  search box - for any perticular website we want make search box on every page the we should make
//the search box as a component. so like this repitedly used functionality can be a component.

//use of components - 1. Reusability
//                    2. maintain the code

//how to build component - we build the component in react by using html, css and js .
//and by combining that component we build the web page.
